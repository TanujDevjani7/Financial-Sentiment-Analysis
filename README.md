This is a little project involving a LSTM model to gauge and predict market sentiment as a boolean output based on text inputs.
It calls a Yahoo Finance API for stock data which it uses to see whether the model can "beat the market" assuming equal access to global financial updates.
Current bottleneck is suficient training data, which remains to be added in. However, the model itself seems to be mathematically sound.

